name: Execute Test Run

on:
  push:
    branches:
      - main

jobs:
  execute-test-run:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Install curl
        run: sudo apt-get install -y curl

      - name: Execute test run
        env:
          IPADDR: 'https://yethi-test.tenjinonline.com'
          INSID: 'yethi-test'
          APITOKEN: ''
          AGENTNAME: 'ponnar'
          INCLUDECLOUDDEVICE: 'FALSE'
          INCLUDECLOUDBROWSER: 'FALSE'
          DEVICENAME: ''
          PROJECTKEY: 'PTO1'
          BROWSER: 'chrome'
          REGION: 'UTC+05:30'
          TESTRUNID: 'Test-Run-12561580'
          ISCLOUDAGENT: 'FALSE'
          BROWSERVERSION: ''
          OS: 'Windows 8.1'
          RUNONLYFAILED: 'FALSE'
        run: |
          echo "Starting Execute test run step"
          # Manually created ID
          MANUAL_ID='Test-Run-12561580'
          echo "Manually created ID: $MANUAL_ID"

          # Execute the test run and capture the automatically generated ID
          EXECUTE=$(curl -s -k --request POST "$IPADDR/api/rest/1/execute/testrun" \
            --header 'Content-Type: application/json' \
            --header "X-INS-ID: $INSID" \
            --header "X-API-TOKEN: $APITOKEN" \
            --data-raw '{
                   "agentName": "'"$AGENTNAME"'",
                   "includeCloudDevice": "'"$INCLUDECLOUDDEVICE"'",
                   "includeCloudBrowser": "'"$INCLUDECLOUDBROWSER"'",
                   "device": "'"$DEVICENAME"'",
                   "projectKey": "'"$PROJECTKEY"'",
                   "browser": "'"$BROWSER"'",
                   "region": "'"$REGION"'",
                   "testRunId": "'"$MANUAL_ID"'",
                   "cloudAgent": "'"$ISCLOUDAGENT"'",
                   "browserVersion": "'"$BROWSERVERSION"'",
                   "os": "'"$OS"'",
                   "reRunFailedCases": "'"$RUNONLYFAILED"'"
                   }')
          echo "EXECUTE response: $EXECUTE"
          AUTO_GENERATED_ID=$(echo $EXECUTE | jq -r '.testRunId')
          echo "Automatically generated ID: $AUTO_GENERATED_ID"
          if [ -z "$AUTO_GENERATED_ID" ] || [ "$AUTO_GENERATED_ID" == "null" ]; then
            echo "Failed to retrieve automatically generated test run ID"
            exit 1
          fi
          
          # Store IDs as environment variables
          echo "MANUAL_ID=$MANUAL_ID" >> $GITHUB_ENV
          echo "AUTO_GENERATED_ID=$AUTO_GENERATED_ID" >> $GITHUB_ENV

      - name: Monitor test run status
        run: |
          echo "Starting Monitor test run status step"
          status=''
          agentstatus=''
          response=''
          result=''
          count=0
          end_time=$(($(date +%s) + 3600)) # One hour timeout
          while [[ $status != 'COMPLETED' && $agentstatus != 'TERMINATED' && $agentstatus != 'ERROR' && $(date +%s) -lt $end_time ]]; do
              echo "Loop count: $count"
              echo "Fetching status for test run ID: $AUTO_GENERATED_ID"
              response=$(curl -s -k --request GET "$IPADDR/api/rest/1/testruns/$AUTO_GENERATED_ID/runstatus" \
                --header 'Content-Type: application/json' \
                --header "X-INS-ID: $INSID" \
                --header "X-API-TOKEN: $APITOKEN")
              echo "API response: $response"
              status=$(echo $response | jq -r '.status')
              result=$(echo $response | jq -r '.result')
              agentstatus=$(echo $response | jq -r '.cloudAgentStatus')
              echo "Parsed status: $status"
              echo "Parsed result: $result"
              echo "Parsed agent status: $agentstatus"
              echo "API response: $response"
              sleep 20
              count=$((count+1))
              if [[ $result == 'FAIL' || $status == 'null' ]]; then
                  echo "Result status is $result"
                  exit 1
              else
                  echo "Result status is $result"
              fi
          done
